import { Store } from "pullstate";

export const UserBalanceStorage = new Store({
    userBalance: 0,
    // addUserBalance: ()=>{},
    updateUserBalance: ()=>{},
    // forceUpdateUserBalance: ()=>{},
});